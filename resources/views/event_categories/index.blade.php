@extends('layouts.app')
@section('title', 'Loại sự kiện')
@section('content')
<div class="d-flex justify-content-between align-items-end mb-4"><div><h1 class="page-title">Danh sách loại sự kiện</h1><div class="page-subtitle">Phân loại sự kiện và quy định điểm tối đa</div></div><a class="btn btn-primary" href="{{ route('event-categories.create') }}"><i class="bi bi-plus-lg me-1"></i>Thêm loại sự kiện</a></div>
<div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Tên loại</th><th>Mô tả</th><th>Điểm tối đa</th><th>Sự kiện</th><th>Quy tắc</th><th>Trạng thái</th><th class="text-end">Thao tác</th></tr></thead><tbody>@forelse($categories as $eventCategory)<tr><td><strong>{{ $eventCategory->name }}</strong></td><td>{{ $eventCategory->description ?: '-' }}</td><td>{{ $eventCategory->max_points }}</td><td>{{ $eventCategory->events_count }}</td><td>{{ $eventCategory->rules_count }}</td><td><span class="badge text-bg-{{ $eventCategory->status === 'active' ? 'success' : 'secondary' }}">{{ $eventCategory->status === 'active' ? 'Hoạt động' : 'Tạm dừng' }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('event-categories.show', $eventCategory) }}"><i class="bi bi-eye"></i></a><a class="btn btn-sm btn-outline-secondary" href="{{ route('event-categories.edit', $eventCategory) }}"><i class="bi bi-pencil"></i></a><form class="d-inline" method="POST" action="{{ route('event-categories.destroy', $eventCategory) }}" onsubmit="return confirm('Xóa loại sự kiện này?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="7" class="empty-state text-center">Chưa có loại sự kiện.</td></tr>@endforelse</tbody></table></div><div class="mt-3">{{ $categories->links() }}</div></div></div>
@endsection
