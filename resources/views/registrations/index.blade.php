@extends('layouts.app')
@section('title', 'Đăng ký sự kiện')
@section('content')
<div class="d-flex justify-content-between align-items-end mb-4"><div><h1 class="page-title">Đăng ký sự kiện</h1><div class="page-subtitle">Dữ liệu sinh viên đăng ký được quản lý và kiểm tra sức chứa sự kiện.</div></div><a class="btn btn-primary" href="{{ route('registrations.create') }}"><i class="bi bi-plus-lg me-1"></i>Tạo đăng ký</a></div>
<div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Sinh viên</th><th>Sự kiện</th><th>CLB</th><th>Ngày đăng ký</th><th>Trạng thái</th><th class="text-end">Thao tác</th></tr></thead><tbody>@forelse($registrations as $registration)<tr><td><strong>{{ $registration->student->full_name }}</strong><div class="small text-secondary">{{ $registration->student->student_code }}</div></td><td>{{ $registration->event->title }}</td><td>{{ $registration->event->club->short_name ?? $registration->event->club->name }}</td><td>{{ $registration->registered_at->format('d/m/Y H:i') }}</td><td><span class="badge text-bg-{{ $registration->status === 'registered' ? 'success' : 'secondary' }}">{{ $registration->status === 'registered' ? 'Đã đăng ký' : 'Đã hủy' }}</span>@if($registration->checkin)<div class="small text-success mt-1">Đã check-in</div>@endif</td><td class="text-end">@if($registration->status === 'registered' && !$registration->checkin)<form class="d-inline" method="POST" action="{{ route('registrations.destroy', $registration) }}" onsubmit="return confirm('Hủy đăng ký này?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Hủy</button></form>@else<span class="text-secondary small">-</span>@endif</td></tr>@empty<tr><td colspan="6" class="empty-state text-center">Chưa có đăng ký.</td></tr>@endforelse</tbody></table></div><div class="mt-3">{{ $registrations->links() }}</div></div></div>
@endsection
