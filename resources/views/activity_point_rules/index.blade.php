@extends('layouts.app')
@section('title', 'Quy tắc điểm')
@section('content')
<div class="d-flex justify-content-between align-items-end mb-4"><div><h1 class="page-title">Bảng quy tắc điểm hoạt động</h1><div class="page-subtitle">Quy định điểm cộng theo từng loại sự kiện</div></div><a class="btn btn-primary" href="{{ route('activity-point-rules.create') }}"><i class="bi bi-plus-lg me-1"></i>Thêm quy tắc</a></div>
<div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Loại sự kiện</th><th>Áp dụng cho</th><th>Điểm cộng</th><th>Mô tả</th><th>Đã áp dụng</th><th class="text-end">Thao tác</th></tr></thead><tbody>@forelse($rules as $activityPointRule)<tr><td>{{ $activityPointRule->category->name }}</td><td>{{ $activityPointRule->event_name ?: 'Toàn bộ loại sự kiện' }}</td><td><strong class="text-primary">{{ $activityPointRule->points }}</strong></td><td>{{ $activityPointRule->description ?: '-' }}</td><td>{{ $activityPointRule->student_points_count }} lần</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('activity-point-rules.show', $activityPointRule) }}"><i class="bi bi-eye"></i></a><a class="btn btn-sm btn-outline-secondary" href="{{ route('activity-point-rules.edit', $activityPointRule) }}"><i class="bi bi-pencil"></i></a><form class="d-inline" method="POST" action="{{ route('activity-point-rules.destroy', $activityPointRule) }}" onsubmit="return confirm('Xóa quy tắc này?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="6" class="empty-state text-center">Chưa có quy tắc điểm.</td></tr>@endforelse</tbody></table></div><div class="mt-3">{{ $rules->links() }}</div></div></div>
@endsection
