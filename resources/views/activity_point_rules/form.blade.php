@include('partials.form-errors')
<form method="POST" action="{{ isset($activityPointRule) ? route('activity-point-rules.update', $activityPointRule) : route('activity-point-rules.store') }}">
@csrf @isset($activityPointRule) @method('PUT') @endisset
<div class="row g-3"><div class="col-md-6"><label class="form-label required">Loại sự kiện</label><select class="form-select" name="event_category_id" required><option value="">-- Chọn loại sự kiện --</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('event_category_id', $activityPointRule->event_category_id ?? '') == $category->id)>{{ $category->name }} (tối đa {{ $category->max_points }} điểm)</option>@endforeach</select></div><div class="col-md-6"><label class="form-label required">Điểm cộng</label><input class="form-control" type="number" min="1" name="points" value="{{ old('points', $activityPointRule->points ?? '') }}" required></div><div class="col-12"><label class="form-label">Mô tả hoặc điều kiện</label><textarea class="form-control" name="description" rows="4">{{ old('description', $activityPointRule->description ?? '') }}</textarea></div></div>
<div class="mt-4 d-flex justify-content-end gap-2"><a class="btn btn-light border" href="{{ isset($activityPointRule) ? route('activity-point-rules.show', $activityPointRule) : route('activity-point-rules.index') }}">Hủy</a><button class="btn btn-primary">Lưu</button></div>
</form>
