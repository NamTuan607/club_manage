@extends('layouts.app')
@section('title', 'Duyệt sự kiện')
@section('content')
<div class="mb-4"><h1 class="page-title">Duyệt sự kiện</h1><div class="page-subtitle">Sự kiện chỉ được phép nhận đăng ký sau khi cán bộ duyệt.</div></div>
<div class="card mb-4"><div class="card-header bg-white"><strong>Sự kiện chờ duyệt</strong></div><div class="card-body"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Sự kiện</th><th>CLB</th><th>Thời gian</th><th>Sức chứa</th><th class="text-end">Xử lý</th></tr></thead><tbody>@forelse($pendingEvents as $event)<tr><td><a href="{{ route('events.show', $event) }}">{{ $event->title }}</a></td><td>{{ $event->club->name }}</td><td>{{ $event->start_time->format('d/m/Y H:i') }}</td><td>{{ $event->capacity }}</td><td class="text-end"><form class="d-inline" method="POST" action="{{ route('events.approve', $event) }}">@csrf<button class="btn btn-sm btn-success">Duyệt</button></form><form class="d-inline" method="POST" action="{{ route('events.reject', $event) }}">@csrf<button class="btn btn-sm btn-outline-danger">Từ chối</button></form></td></tr>@empty<tr><td colspan="5" class="empty-state text-center">Không có sự kiện chờ duyệt.</td></tr>@endforelse</tbody></table></div></div></div>
<div class="card"><div class="card-header bg-white"><strong>Lịch sử phê duyệt</strong></div><div class="card-body"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Sự kiện</th><th>Cán bộ</th><th>Thời điểm</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($approvals as $eventApproval)<tr><td>{{ $eventApproval->event->title }}</td><td>{{ $eventApproval->approver->name }}</td><td>{{ $eventApproval->approved_at?->format('d/m/Y H:i') }}</td><td><span class="badge text-bg-{{ $eventApproval->status === 'approved' ? 'success' : 'danger' }}">{{ $eventApproval->status === 'approved' ? 'Đã duyệt' : 'Từ chối' }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('event-approvals.show', $eventApproval) }}"><i class="bi bi-eye"></i></a></td></tr>@empty<tr><td colspan="5" class="empty-state text-center">Chưa có lịch sử duyệt.</td></tr>@endforelse</tbody></table></div><div class="mt-3">{{ $approvals->links() }}</div></div></div>
@endsection
