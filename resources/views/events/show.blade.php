@extends('layouts.app')
@section('title', $event->title)
@section('content')
<div class="d-flex justify-content-between align-items-start mb-4"><div><h1 class="page-title">{{ $event->title }}</h1><div class="page-subtitle">{{ $event->club->name }} · {{ $event->category->name }}</div></div><div class="d-flex gap-2">@if($event->status === 'pending')<a class="btn btn-success" href="{{ route('event-approvals.create', ['event_id' => $event->id]) }}"><i class="bi bi-check2-square me-1"></i>Duyệt</a>@endif<a class="btn btn-primary" href="{{ route('events.edit', $event) }}"><i class="bi bi-pencil me-1"></i>Sửa</a></div></div>
<div class="row g-4"><div class="col-lg-7"><div class="card h-100"><div class="card-body"><h2 class="h6 mb-3">Thông tin chung</h2><dl class="row"><dt class="col-sm-4">Thời gian</dt><dd class="col-sm-8">{{ $event->start_time->format('d/m/Y H:i') }} - {{ $event->end_time->format('d/m/Y H:i') }}</dd><dt class="col-sm-4">Địa điểm</dt><dd class="col-sm-8">{{ $event->location }}</dd><dt class="col-sm-4">Sức chứa</dt><dd class="col-sm-8">{{ $event->registrations->where('status', 'registered')->count() }}/{{ $event->capacity }} đăng ký</dd><dt class="col-sm-4">Trạng thái</dt><dd class="col-sm-8">{{ $event->status }}</dd></dl><h2 class="h6 mt-4">Mô tả</h2><p class="mb-0" style="white-space:pre-line">{{ $event->description ?: 'Chưa có mô tả.' }}</p></div></div></div><div class="col-lg-5"><div class="card mb-4"><div class="card-body"><h2 class="h6">Lịch sử duyệt</h2>@forelse($event->approvals as $approval)<div class="border-bottom py-2"><strong>{{ $approval->status === 'approved' ? 'Đã duyệt' : 'Từ chối' }}</strong><div class="small text-secondary">{{ $approval->approver->name }} · {{ $approval->approved_at?->format('d/m/Y H:i') }}</div><div class="small">{{ $approval->note }}</div></div>@empty<div class="text-secondary">Chưa có kết quả duyệt.</div>@endforelse</div></div><div class="card"><div class="card-body"><h2 class="h6">Đăng ký và check-in</h2><div>Đăng ký: <strong>{{ $event->registrations->where('status', 'registered')->count() }}</strong></div><div>Check-in: <strong>{{ $event->registrations->filter(fn($item) => $item->checkin)->count() }}</strong></div><a class="btn btn-sm btn-outline-primary mt-3" href="{{ route('registrations.index') }}">Xem danh sách đăng ký</a></div></div></div></div>
@endsection
