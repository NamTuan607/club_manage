@extends('layouts.app')
@section('title', 'Điểm hoạt động sinh viên')
@section('content')
<div class="mb-4"><h1 class="page-title">Điểm hoạt động sinh viên</h1><div class="page-subtitle">Điểm được tạo tự động khi cán bộ duyệt check-in.</div></div>
<div class="row g-3 mb-4">@forelse($studentTotals as $total)<div class="col-md"><div class="card h-100"><div class="card-body"><div class="text-secondary small">{{ $total->student->full_name }}</div><div class="stat-value">{{ $total->total_points }}</div><div class="small text-secondary">điểm tích lũy</div></div></div></div>@empty<div class="col"><div class="text-secondary">Chưa có điểm hoạt động.</div></div>@endforelse</div>
<div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Sinh viên</th><th>Sự kiện</th><th>Quy tắc</th><th>Điểm</th><th>Ngày cộng</th><th>Chứng nhận</th><th></th></tr></thead><tbody>@forelse($studentPoints as $studentPoint)<tr><td>{{ $studentPoint->student->full_name }}</td><td>{{ $studentPoint->event->title }}</td><td>{{ $studentPoint->rule->event_name ?: $studentPoint->rule->category->name }}</td><td><strong class="text-primary">{{ $studentPoint->points }}</strong></td><td>{{ $studentPoint->awarded_at->format('d/m/Y H:i') }}</td><td>@if($studentPoint->certificate)<a href="{{ route('certificates.show', $studentPoint->certificate) }}">{{ $studentPoint->certificate->certificate_code }}</a>@else <a class="btn btn-sm btn-outline-success" href="{{ route('certificates.create', ['student_point_id' => $studentPoint->id]) }}">Cấp chứng nhận</a>@endif</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('student-points.show', $studentPoint) }}"><i class="bi bi-eye"></i></a></td></tr>@empty<tr><td colspan="7" class="empty-state text-center">Chưa có điểm hoạt động.</td></tr>@endforelse</tbody></table></div><div class="mt-3">{{ $studentPoints->links() }}</div></div></div>
@endsection
